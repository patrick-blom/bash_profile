#.bash_profile
My personal bash profile configuration.

##Branchlist
 * master : designed for os x
 * linux : designed for linux

##Installation
Clone the branch for your OS and mv the `bash_profile` file to your home directory. To load the file at terminal start up
just rename the filename from `bash_profile` to `.bash_profile` and reload the terminal.